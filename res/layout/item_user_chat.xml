<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp">
    
    <ImageView
        android:id="@+id/item_user_imageview"
        android:layout_alignParentRight="true"
        android:background="@drawable/img_bg"
        android:layout_marginLeft="5dp"
	    android:layout_width="50dp"
	    android:layout_height="50dp"/>
    
    <TextView
        android:id="@+id/item_user_message_textview"
        android:layout_toLeftOf="@id/item_user_imageview"
        android:layout_alignParentTop="true"
        android:gravity="right"
        android:layout_marginTop="2dp"
        android:textSize="16sp"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"/>
    
    <TextView
        android:id="@+id/item_user_timestamp_textview"
        android:layout_toLeftOf="@id/item_user_imageview"
        android:layout_alignParentBottom="true"
        android:layout_below="@id/item_user_message_textview"
        android:layout_marginTop="2dp"
        android:textSize="12sp"
        android:textColor="@color/Gray"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"/>\
	
</RelativeLayout>
